synse-server-dev:
  container_name: synse-server-dev
  build: ..
  dockerfile: dockerfile/release.dockerfile
  links:
    - ipmi-emulator

ipmi-emulator:
  build: ../synse/emulator/ipmi
  dockerfile: Dockerfile.x64
  ports:
    - 623:623/udp
  environment:
    - VAPOR_DEBUG=true