version: "2"
services:
  synse-server:
    container_name: synse-server
    build:
      context: ..
      dockerfile: dockerfile/release.dockerfile
    ports:
      - 5000:5000
    environment:
      - VAPOR_DEBUG=true
