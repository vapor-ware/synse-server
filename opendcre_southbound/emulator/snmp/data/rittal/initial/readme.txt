This directory contains snmp-emulator configuration data for the initial walks we got from Rittal.
.snmprec files are snmp emulator data where the name of the file before .snmprec is the SNMP community string.
