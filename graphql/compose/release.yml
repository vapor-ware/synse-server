version: "2"
services:
  release:
    container_name: synse_graphql
    image: vaporio/synse_graphql
    build:
      context: ..
      dockerfile: dockerfile/release.dockerfile
    ports:
      - "5001:5001"
