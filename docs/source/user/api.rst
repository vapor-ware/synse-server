.. _api:

API Reference
=============
The API docs are not currently hosted. To build and view them locally, clone the
repo and run

.. code-block:: bash

    # build the docs
    make docs

    # view the docs
    make api-docs
